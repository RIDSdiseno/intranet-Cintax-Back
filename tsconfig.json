{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "rootDir": "src",            // 👈 ahora solo compila lo de /src
    "outDir": "./dist",          // 👈 genera dist/server.js, dist/app.js, etc.
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": [
    "src/**/*.ts"                // 👈 solo código de la app
  ],
  "exclude": [
    "node_modules",
    "dist",
    "prisma"                     // 👈 los seeds los ejecutas con ts-node/tsx
  ]
}
